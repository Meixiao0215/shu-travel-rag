# 验证记录

日期：2026-09-09。代码运行于项目独立 `.venv`，没有修改原 PDF 或原有其他项目环境。

## 已实际运行

- 检查全部12页文本，并渲染核对第3、4、7、8、10页，重点检查表格和例外条款。
- 从原 PDF 导入得到33条正文、1份通知、2张附表，共37个检索小块。
- 使用本地缓存 BGE-M3 编码并持久化 Chroma 索引。再次执行索引任务新增0条，确认复用。
- `pip check`：无损坏的依赖关系；版本已记录到 `requirements.lock`。
- 核心与页面测试20项通过；DeepSeek HTTP适配器契约测试2项通过。
- BM25、向量、混合检索在27道开发集可回答单轮题上完成评测，详见 `eval/README.md`。
- Streamlit 本地服务成功启动，绑定 `127.0.0.1:8501`。页面测试覆盖无密钥启动及关键词搜索。

## 测试替身覆盖的范围

Agent 测试使用可控模型输出，实际执行 LangChain 的 `create_agent` 工具循环。适配器测试使用 HTTP 替身检查实际 `ChatDeepSeek` 序列化出的请求，包括模型名、思考模式关闭、工具结果回传和普通RAG的JSON模式。

这些测试证明代码接口和控制流程能够配合，不证明 DeepSeek 服务当前认证成功、网络可达，也不证明真实模型生成内容正确。

## 等待本地密钥配置的验证

1. 在 `.env` 填写密钥，重启已运行的页面服务。
2. 执行 `python -m travel_rag.cli ask "横向科研项目自驾汽油费能报销吗？"`。
3. 分别演示直接问答、会议多轮追问、缺少条件和境外问题。
4. 用 `eval --generate` 导出样本，对照原文填写人工评阅项。

没有把未执行的真实模型调用、留出测试集或生成质量评测标记为通过。
